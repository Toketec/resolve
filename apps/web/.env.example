# ──────────────────────────────────────────
# @resolve/web — 环境变量模板
# ──────────────────────────────────────────
# 复制此文件为 .env.local 并填入真实值。
# Supabase 凭据从根 .env 读取（由 @resolve/db 自动加载）。
#
# NEXT_PUBLIC_ 变量会在 Next.js 构建时编译进浏览器端 JS，
# 这意味着页面刷新后前端自己就能直接调用 Supabase anon API。
# 安全由 Supabase Row Level Security (RLS) 策略保障。

# Supabase（已注册项目 resolve，ID: vrubfcgxxxxnbkurmoaj）
# SUPABASE_URL=https://vrubfcgxxxxnbkurmoaj.supabase.co
# SUPABASE_ANON_KEY=

# ── 合约地址（Shasta 测试网）────────────────────────────
# NEXT_PUBLIC_SETTLEMENT_ADDRESS=TRpc78jLEq2wsxuB1mCvqSahU47B73UoVV
# NEXT_PUBLIC_USDD_ADDRESS=TYAw3pLComCus5SVxgGhxVbspqNj3PYDHY

# ── Tron 网络 ──────────────────────────────────────────
# NEXT_PUBLIC_TRON_FULL_HOST=https://api.shasta.trongrid.io

# ── 气囊模式（默认 true）───────────────────────────────
# true → 结算走 settleSimulated（标记已结算，不转账）
# false → 真实赔付，赢家收到 USDD（需要合约有余额）
# NEXT_PUBLIC_AIRBAG_ENABLED=true

# ── 服务端签名（仅 settle API 使用）───────────────────
# 部署合约的 owner 地址私钥，用于签 settle/settleSimulated 交易
# TRON_PRIVATE_KEY=
