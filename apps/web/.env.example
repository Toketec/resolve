# ──────────────────────────────────────────
# @resolve/web — 环境变量模板
# ──────────────────────────────────────────
# 复制此文件为 .env.local 并填入真实值。
# 所有值由你在创建 Supabase 项目和部署合约后获得。
#
# NEXT_PUBLIC_ 变量会在 Next.js 构建时编译进浏览器端 JS。
# 安全由 Supabase Row Level Security (RLS) 策略保障。
#
# 如何获取这些值 → 见 docs/deployment-plan.zh.md

# ── Supabase（创建项目后获得）───────────────────────────
# 在 supabase.com 创建项目 → Settings → API
# SUPABASE_URL=
# SUPABASE_ANON_KEY=

# ── 合约地址（Shasta 测试网）────────────────────────────
# 部署合约后从部署脚本输出中获得
# NEXT_PUBLIC_SETTLEMENT_ADDRESS=
# NEXT_PUBLIC_USDD_ADDRESS=

# ── Tron 网络 ──────────────────────────────────────────
# 默认使用 Shasta 测试网 RPC
# NEXT_PUBLIC_TRON_FULL_HOST=https://api.shasta.trongrid.io


# ── Agent 链上身份模式 ────────────────────────────────
# mock → 派生串（默认） | preconfig → 预设 TRON 地址 | live → 链上实时读取
# NEXT_PUBLIC_AGENT_REGISTRY_MODE=preconfig
# AgentRegistry 合约地址（live 模式需要）
# NEXT_PUBLIC_AGENT_REGISTRY_ADDRESS=

# ── 服务端签名（仅 settle API 使用）───────────────────
# 部署合约的 owner 地址私钥，用于签 settle/settleSimulated 交易
# TRON_PRIVATE_KEY=
