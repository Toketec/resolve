# ──────────────────────────────────────────
# @resolve/web — 环境变量模板
# ──────────────────────────────────────────
# 复制此文件为 .env.local 并填入真实值。
# Supabase 凭据从根 .env 读取（由 @resolve/db 自动加载）。
#
# NEXT_PUBLIC_ 变量会在 Next.js 构建时编译进浏览器端 JS，
# 这意味着页面刷新后前端自己就能直接调用 Supabase anon API。
# 安全由 Supabase Row Level Security (RLS) 策略保障。

# Supabase（已注册项目 resolve，ID: vrubfcgxxxxnbkurmoaj）
# SUPABASE_URL=https://vrubfcgxxxxnbkurmoaj.supabase.co
# SUPABASE_ANON_KEY=
