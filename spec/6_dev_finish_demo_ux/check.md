# ✅ Spec 6 — Demo & UX Polish 验收检查表

> **用途**: 验证 Demo 演示效果、UX 打磨、容错处理
> **执行人**: 开发完成后由 AI 自检 + 人工真人模拟演示
> **检查项分布**: 6 核心演示功能 + 3 容错保护 + 2 B.AI/x402 展示 + 6 经济扩展加分

---

## 🔧 AI 自检（每次提交前运行）

```bash
pnpm typecheck || exit 1
pnpm build      || exit 1

# 确认 USDC 无残留
grep -rn 'USDC' apps/web --include='*.tsx' --include='*.ts' | grep -v 'node_modules' | grep -v 'USDD' | grep -v '//.*USDC'
```

**失败则停止提交，先修复。**

---

## 🧪 检查点

> 前提: spec 5 已跑通，`pnpm dev` 运行中，Chrome 已装 TronLink（Shasta 测试网）。

---

### 🔵 核心演示功能（6 项 — 必须通过）

| # | 操作步骤 | 预期结果 | 备注 |
|:-:|----------|----------|:----:|
| **6.1** | 打开 `/markets/btc-150k-2026?dev=1` | 页面出现 "⚡ Force Resolve (dev only)" 按钮 | G5: dev 参数显示按钮 |
| **6.2** | 打开 `/markets/btc-150k-2026`（不加 ?dev=1） | 看不到 Force Resolve 按钮 | G5: 正常用户不暴露 |
| **6.3** | 点击 Force Resolve | Market 状态立即变为 `resolving` | resolve API 被调用 |
| **6.4** | 观察 vote 动画 | 3 条 votes 每条间隔 ~1.5s 依次淡入上移出现 | G4: 浮现动画 |
| **6.5** | 观察 ConsensusMeter | 进度条随每条 vote 逐步增长更新 | 动画跟随 vote 节奏 |
| **6.6** | 全部 3 条 votes 出现后 | status → "consensus"；ConsensusMeter 显示 ✓ 完成标记 | 共识达成标志 |

---

### 🟡 容错保护（3 项 — 强烈推荐通过）

| # | 操作步骤 | 预期结果 | 备注 |
|:-:|----------|----------|:----:|
| **6.7** | 检查 TradePanel | 显示 "USDD" 而非 "USDC" | G3: USDC→USDD 替换 |
| **6.8** | 全局检查 USDC 残留 | `grep -rn USDC apps/web --include='*.tsx' --include='*.ts'` 无反应该出现之项目（除注释说明） | 确保无遗漏 |
| **6.9** | 断开网络 → 点击任意操作按钮 | 显示友好错误提示 + "Retry" 按钮，页面不崩溃 | 网络容错 |
| **6.10** | 正常网络下买入 | 按钮显示 "Processing..." + 禁用状态；完成后恢复正常 | Loading 状态 |
| **6.11** | 在市场详情页加载时 | 显示骨架屏 / spin indicator，非空白闪烁 | 骨架屏加载态 |

---

### 🟢 B.AI / x402 展示（2 项 — 加分项）

| # | 操作步骤 | 预期结果 | 备注 |
|:-:|----------|----------|:----:|
| **6.12** 🆕 | 检查 Agent 8004 卡片 | 页面某处显示 8004 ID，点击可跳转 Tronscan | B.AI 身份展示 |
| **6.13** 🆕 | Consensus 达成后检查 x402 区域 | 显示交易哈希 + 链上跳转链接 | x402 支付展示 |

---

### 🟣 经济扩展加分（6 项 — HTX 生态加分）

| # | 操作步骤 | 预期结果 | 对应需求 |
|:-:|----------|----------|:--------:|
| **6.14** 🆕 | TradePanel 底部查找 $HTX 信息 | 显示 "0.1% fee → $HTX Buyback" + 累计回购数量 | C-20 Buyback |
| **6.15** 🆕 | 结算后查看 Agent 卡片 | 显示 "0.0X $HTX earned" 激励数据 | C-20 Agent 激励 |
| **6.16** 🆕 | 市场详情页查找订单簿区域 | 可见买单/卖单深度图（HTX API 实时数据） | C-21 订单簿 |
| **6.17** 🆕 | 市场详情页查找 K 线区域 | 可见 HTX 真实 BTC K 线走势（蜡烛图/线图） | C-21 K 线 |
| **6.18** 🆕 | 浏览各 Agent 卡片 | 卡片上可见 "Powered by B.AI" 算力徽章 | C-22 算力徽章 |
| **6.19** 🆕 | 点击 "Powered by B.AI" 徽章 | 跳转至 B.AI 平台介绍页 | C-22 链接验证 |

---

## 📋 验收通过标准

| 层级 | 范围 | 要求 |
|:----|------|:----:|
| ⭐ **P0 核心** | 6.1–6.6（投票动画 + 隐藏触发） | ✅ **必须通过** |
| ⭐ **P1 容错** | 6.7–6.11（USDD 替换 + Loading + Error） | ✅ **推荐全部通过** |
| ⭐ **P2 展示** | 6.12–6.13（B.AI + x402） | ⭕ 加分项，推荐通过以提升展示分 |
| ⭐ **P3 经济生态** | 6.14–6.19（HTX Buyback/激励/图表/徽章） | ⭕ 加分项，提升生态分 |

**最终判定**: 6.1–6.6 全部通过 → **核心演示能力达标 ✓**

---

## 🎬 Hero Market 演示检查（与根 check.md H1-H13 联动）

在 `docs/ENG/check.md` 的 **Hero Market 终验** 章节中运行整体演示，本 spec 主要负责 **H4–H6** 的用户体验部分：

| Hero # | 关联本 spec 检查点 | 体验要素 |
|:------:|:-----------------:|----------|
| H4 | 6.1–6.2 | Force Resolve 隐藏触发 |
| H5 | 6.3–6.4 | 投票浮现动画（1.5s/条） |
| H6 | 6.5–6.6 | ConsensusMeter 进度条 + ✓ 标记 |
| H9–H13 | 6.12–6.19 | B.AI / x402 / HTX 经济展示 |
