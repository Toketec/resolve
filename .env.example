# ──────────────────────────────────────────
# RESOLVE — 环境变量模板
# ──────────────────────────────────────────
# 复制此文件为 .env 或 .env.local 并填入真实值。
# .env / .env.local 已在 .gitignore 中，不会被提交。
# 
# 所有值由你创建服务和部署合约后获得。
# 如何获取 → 见 docs/deployment-plan.zh.md

# ── Supabase（创建项目后获得）───────────────────────────
# 在 supabase.com 创建项目 → Settings → API 获取
# SUPABASE_URL=
# SUPABASE_ANON_KEY=

# ── LLM Provider（OpenAI 兼容端点）─────────────────────
# 可指向 OpenRouter / DeepSeek / B.AI / Claude relay 等
# OPENAI_API_KEY            API 密钥
# OPENAI_BASE_URL           API 端点（如 https://openrouter.ai/api/v1）
# OPENAI_MODEL              模型名（默认 gpt-5.5）

# ── 合约地址（Shasta 测试网）────────────────────────────
# 部署合约后从部署脚本输出中获得
# NEXT_PUBLIC_SETTLEMENT_ADDRESS=
# NEXT_PUBLIC_USDD_ADDRESS=

# ── Tron 网络 ──────────────────────────────────────────
# NEXT_PUBLIC_TRON_FULL_HOST=https://api.shasta.trongrid.io

# ── 气囊模式（默认 true）───────────────────────────────
# NEXT_PUBLIC_AIRBAG_ENABLED=true

# ── 服务端签名（仅 settle API 使用）───────────────────
# 部署合约的 owner 地址私钥
# TRON_PRIVATE_KEY=

# ── Agent 链上身份注册 ────────────────────────────────
# 模式: mock | preconfig | live （默认 mock）
# NEXT_PUBLIC_AGENT_REGISTRY_MODE=preconfig
# AgentRegistry 合约地址（live 模式需要）
# NEXT_PUBLIC_AGENT_REGISTRY_ADDRESS=
