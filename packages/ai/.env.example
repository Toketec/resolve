# @resolve/ai — 环境变量模板（复制为 .env，绝不提交）

# OpenAI 兼容端点（默认；可指向 relay / B.AI / 官方 OpenAI）
OPENAI_API_KEY=sk-...
OPENAI_BASE_URL=https://relay.zijo.io
OPENAI_MODEL=gpt-5.5

# 可选：B.AI（如兼容 OpenAI Chat 格式，复用上面的 OPENAI_* 即可）
# BAI_API_KEY=
# BAI_API_ENDPOINT=
