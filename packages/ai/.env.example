# @resolve/ai — 环境变量模板（复制为 .env，绝不提交）
#
# 所有值由你选择 LLM 提供商后获得。
# 无 OPENAI_API_KEY 时 AI 推理自动降级为 mock 兜底（不崩溃）。

# OpenAI 兼容端点（必填 — API 密钥；URL 和 Model 可选）
# OPENAI_API_KEY=
# OPENAI_BASE_URL=
# OPENAI_MODEL=

# 可选：B.AI（如兼容 OpenAI Chat 格式，复用上面的 OPENAI_* 即可）
# BAI_API_KEY=
# BAI_API_ENDPOINT=
